1. args[0] - файл откуда читаем

2. Читаем файл сохраняем информацию в ArrayList<Operation>

3. Ищем в этом листе элементы с одинаковыми точками продаж,
   группируем по ним (подумать какую структуру данных взять),
   в группах считаем сумму

4.
